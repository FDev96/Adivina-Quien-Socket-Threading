server {
    listen 81 default_server;
    server_name _;

    location / {
        default_type text/plain;
        return 200 "Nginx base config on port 81. Replace nginx/conf.d/default.conf with your own configuration.\n";
    }
}
